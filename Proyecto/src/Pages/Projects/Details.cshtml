@page
@model Proyecto.Pages_Projects.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h2>Details</h2>

<div>
    <h4>Project</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Project.Title)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Project.Title)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Project.Description)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Project.Description)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Project.StartDate)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Project.StartDate)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Project.EndDate)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Project.EndDate)
        </dd>
    </dl>
</div>

<div>
    @if(Model.Technician != null)
    {
        string technicianToAddID = Model.Technician.Id;
        string technicianToDeleteID = Model.Technician.Id;
        <form method="post" asp-page-handler="AddPostulate"
        asp-route-id="@Model.Project.ProjectID" asp-route-technicianToAddID="@technicianToAddID">
        <button class="btn btn-success">Suscribe</button>
        </form>
        
        <form method="post" asp-page-handler="UnPostulateTechnician"
                                asp-route-id="@Model.Project.ProjectID" asp-route-technicianToDeleteID="@technicianToDeleteID">
                                <button class="btn btn-danger">Unsuscribe</button>
                            </form>
    }
    
    <a asp-page="./Index">Back to List</a>
</div>
