@page
@model Proyecto.Pages.Roles.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Role[0].RolDescription)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Role[0].Level)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Role) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RolDescription)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Level.RolLevelDescription)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.RoleId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.RoleId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.RoleId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
